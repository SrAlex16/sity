[pytest]
testpaths = tests
markers =
    behavior_regression: Tests that call the real Haiku model to catch behavior regressions. Run manually before large deploys. Exclude with -m "not behavior_regression".
